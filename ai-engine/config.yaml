paths:
  detection_model: "models/detection/best.pt"
  pose_model: ""

models:
  detection:
    confidence_threshold: 0.6    # минимальная уверенность для детекции (0-1)
    iou_threshold: 0.7           # порог для подавления дубликатов (0-1)

logging:
  level: "INFO"